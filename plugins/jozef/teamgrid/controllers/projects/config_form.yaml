# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Project

# Model Form Field configuration
form: $/jozef/teamgrid/models/project/fields.yaml

# Model Class name
modelClass: Jozef\Teamgrid\Models\Project

# Default redirect location
defaultRedirect: jozef/teamgrid/projects

# Create page
create:
    title: backend::lang.form.create_title
    redirect: jozef/teamgrid/projects/update/:id
    redirectClose: jozef/teamgrid/projects

# Update page
update:
    title: backend::lang.form.update_title
    redirect: jozef/teamgrid/projects
    redirectClose: jozef/teamgrid/projects

# Preview page
preview:
    title: backend::lang.form.preview_title
