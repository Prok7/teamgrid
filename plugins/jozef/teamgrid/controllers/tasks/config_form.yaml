# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Task

# Model Form Field configuration
form: $/jozef/teamgrid/models/task/fields.yaml

# Model Class name
modelClass: Jozef\Teamgrid\Models\Task

# Default redirect location
defaultRedirect: jozef/teamgrid/tasks

# Create page
create:
    title: backend::lang.form.create_title
    redirect: jozef/teamgrid/tasks/update/:id
    redirectClose: jozef/teamgrid/tasks

# Update page
update:
    title: backend::lang.form.update_title
    redirect: jozef/teamgrid/tasks
    redirectClose: jozef/teamgrid/tasks

# Preview page
preview:
    title: backend::lang.form.preview_title
